import { Button } from "flowbite-react";

export const Picker = () => {
    return (
        <div>
            <div>
                <Button>Pass</Button>
                <Button>Undo</Button>
                <Button>Reset</Button>
            </div>
        </div>
    );
};
